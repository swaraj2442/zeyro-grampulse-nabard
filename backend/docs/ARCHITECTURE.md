# Architecture

The authoritative architecture reference is currently maintained in the repository root as `Architecture V2.md`.

This `docs/` version should become the implementation-aligned architecture summary as the codebase matures.

